# Architecture

This document has moved to [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md), which is now the canonical, maintained version. It defines the application's module boundaries (component registry, schema, editor configuration, preview rendering, export rendering, design tokens, accessibility utilities, persistence, media storage, validation, and Rise/LMS communication).

This file is kept only as a redirect so existing links do not break.
