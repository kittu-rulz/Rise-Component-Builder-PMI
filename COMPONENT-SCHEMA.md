# Component schema

This document has moved to [`docs/COMPONENT-SCHEMA.md`](docs/COMPONENT-SCHEMA.md), which is now the canonical, maintained version.

This file is kept only as a redirect so existing links do not break.
